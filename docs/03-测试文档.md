# DailyPicks（今日精选）— 测试文档

- 版本：v1.0 ｜ 日期：2026-08-27
- 配套文档：`01-设计文档.md`、`02-开发文档.md`
- 目标：**任何实现只要跑通本测试文档，即可认定为满足设计与开发文档的要求。**

---

## 1. 测试策略

| 层级 | 范围 | 工具 | 目标 |
|---|---|---|---|
| 单元测试 | 单模块（config/storage/ranker/digest/feedback…） | pytest | 覆盖核心逻辑分支 |
| 集成测试 | 模块间协作（采集→入库→排序→推送），**所有 HTTP 用 respx mock** | pytest + respx + pytest-asyncio | 验证数据流正确性 |
| E2E | `cli.run_once` 全流程（mock 外部 HTTP） | pytest + respx | 一测到底，含降级路径 |
| 手动验收 | 真实外部调用（DeepSeek/微信 webhook） | 命令行 | 上线前最后一关（§7） |

- 覆盖率目标：核心模块（storage/ranker/llm/digest/publisher/feedback/sources）**≥85%**（`coverage report --fail-under 85`）。
- 外部依赖一律 mock：**测试过程不允许真实网络请求**（respx 默认拦截所有 httpx 请求，未匹配路由直接断言失败）。
- 时间相关用例用 `freezegun` 冻结。

### 1.1 运行命令

```bash
uv run pytest                          # 全量
uv run pytest -k "rank"               # 按模块
uv run pytest --cov --cov-report=term-missing   # 覆盖率
uv run ruff check .
```

---

## 2. 测试环境

```bash
uv add --dev pytest pytest-asyncio respx freezegun pytest-cov ruff socksio
```

- **`socksio` 是必需的**：httpx `trust_env=True` 会读取 shell 的 `all_proxy`；本机开发常设
  `all_proxy=socks5://127.0.0.1:7890`，缺 `socksio` 时构造 `AsyncClient` 抛 `ImportError`（用例假性失败）。
  装了 socksio 后，带代理环境跑 `uv run pytest` 也应全绿。

`tests/conftest.py` 必备内容：

```python
import pytest, respx
from httpx import AsyncClient

@pytest.fixture
def tmp_db(tmp_path):            # 返回 Storage(tmp_path / "test.db")，已 init_schema
    ...

@pytest.fixture
def sample_config(tmp_path):     # 返回 RootConfig（全默认值，storage.db_path 指向 tmp）
    ...

@pytest.fixture
def mock_http():                 # respx.mock(assert_all_called=False) 上下文，供各用例注册路由
    with respx.mock(assert_all_mocked=True) as m:
        yield m

@pytest.fixture
def frozen_now():                # freezegun.freeze_time("2026-08-27 08:00:00", tz_offset=8)
    ...
```

---

## 3. 测试数据（tests/fixtures/）

### 3.1 `rss_sample.xml`（2 条）

```xml
<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0"><channel><title>Sample</title>
  <item>
    <title>AI 编程工具实战</title>
    <link>https://example.com/post/1</link>
    <description>&lt;p&gt;用 AI 写代码的十个技巧，亲测有效。&lt;/p&gt;</description>
    <pubDate>Wed, 26 Aug 2026 08:00:00 GMT</pubDate>
    <author>张三</author>
    <guid>https://example.com/post/1</guid>
  </item>
  <item>
    <title>前端性能优化指南</title>
    <link>https://example.com/post/2</link>
    <description>浏览器渲染原理与优化实践。</description>
    <pubDate>Tue, 25 Aug 2026 08:00:00 GMT</pubDate>
    <guid>https://example.com/post/2</guid>
  </item>
</channel></rss>
```

### 3.2 `bilibili_sample.json`（截取自真实响应，字段完整）

```json
{"code":0,"message":"0","ttl":1,"data":{"list":[
  {"aid":117155953776818,"bvid":"BV1wwhG6JEnc","title":"世界伊始——《伊莫》全球上线定档",
   "desc":"PC端9月16日，移动端9月23日！","owner":{"mid":3546868241926891,"name":"伊莫"},
   "stat":{"view":3832818},"pubdate":1787702400}
]}}
```

### 3.3 `zhihu_sample.json`

```json
{"data":[
  {"type":"hot_list_feed","target":{
     "id":2075969425702741132,"title":"西藏日喀则吉隆口岸发生泥石流，致 3 人遇难 558 人失联",
     "url":"https://api.zhihu.com/questions/2075969425702741132",
     "excerpt":"最新消息 8月26日，西藏日喀则市吉隆县发生泥石流灾害。",
     "answer_count":1088,"created":1787729680}}
]}
```

### 3.4 `juejin_sample.json`

```json
{"err_no":0,"err_msg":"success","data":[
  {"item_type":2,"item_info":{
     "article_id":"7637856870833635343",
     "article_info":{"article_id":"7637856870833635343","title":"Cursor 转 Codex 大半个月，聊聊我的真实感受",
                     "brief_content":"Cursor 让我第一次强烈感受到 AI 编程的震撼，Codex 则让我真正感觉到 AI 是超级 Agent。","ctime":1778379468},
     "author_user_info":{"user_name":"深小乐"}}}
]}
```

### 3.5 Hacker News（Algolia 格式）

```json
{"hits":[
  {"objectID":"49448321","title":"AWS Acquires DuckLabs","url":"https://ducklabs.com/news/2026/08/26/ducklabs-to-join-aws",
   "author":"onderkalaci","created_at":"2026-08-26T12:59:26Z","points":120},
  {"objectID":"49400000","title":"Ask HN: 如何入门 Rust？","url":null,
   "author":"rustfan","created_at":"2026-08-26T10:00:00Z","points":88}
]}
```

### 3.6 InfoQ（RSS，与 3.1 同构）

```xml
<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0"><channel><title>InfoQ</title>
  <item>
    <title>Aspire 13.5 正式发布</title>
    <link>https://www.infoq.cn/article/1</link>
    <description>终端进入仪表板，TypeScript AppHost 转正。</description>
    <pubDate>Thu, 27 Aug 2026 08:00:00 GMT</pubDate>
    <guid>https://www.infoq.cn/article/1</guid>
  </item>
</channel></rss>
```

### 3.7 LLM 响应

- `llm_response_ok.json`：`{"choices":[{"message":{"content":"{\"picks\":[{\"article_id\":1,\"rank\":1,\"reason\":\"最符合AI兴趣\"}]}"},"usage":{"prompt_tokens":1000,"completion_tokens":80}}]}`（含 usage）
- `llm_response_badjson.txt`：`{"choices":[{"message":{"content":"抱歉，我无法完成"}}]}`（content 非 JSON）
- `llm_response_fence.txt`：content 为 ```json\n{"picks":[]}\n```（带围栏，应能剥离）

### 3.8 推送响应

- `wecom_ok.json`：`{"errcode":0,"errmsg":"ok"}`
- `wecom_badkey.json`：`{"errcode":93000,"errmsg":"invalid webhook key"}`
- `serverchan_ok.json`：`{"code":0,"message":"","data":{}}`

---

## 4. 测试用例

### 4.1 配置模块（test_config.py）

| ID | 级别 | 描述 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|---|
| T-CFG-01 | 单元 | 默认配置加载 | 无 config.yaml | `load_config()` | 抛 `ConfigError`（文件不存在） |
| T-CFG-02 | 单元 | 合法配置加载 | 临时目录写最小 YAML | `load_config(path)` | 返回 RootConfig，缺省字段取默认值 |
| T-CFG-03 | 单元 | top_n > max_candidates 校验 | YAML 中 top_n=50, max_candidates=40 | `load_config` | 抛 `ConfigError` |
| T-CFG-04 | 单元 | 非法 provider | provider="sms" | `load_config` | 抛 `ConfigError` |
| T-CFG-05 | 单元 | 未知源 key 容忍 | sources.enabled=["rss","foo"] | `load_config` | 成功；警告日志含 foo |
| T-CFG-06 | 单元 | 密钥缺失 | 无 DEEPSEEK_API_KEY 环境变量 | `LLMConfig().api_key` | 抛 `ConfigError` |
| T-CFG-07 | 单元 | init 生成默认配置 | 空目录 | `write_default_config(path)` 后 load | 两次加载结果等价；文件含 `push.provider: wecom` |
| T-CFG-08 | 单元 | 关键词权重越界 | keywords 权重 100 | `load_config` | 抛 `ConfigError`（>10） |

### 4.2 存储模块（test_storage.py）

| ID | 级别 | 描述 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|---|
| T-DB-01 | 单元 | init_schema 幂等 | tmp_db | 连续调用两次 | 无异常 |
| T-DB-02 | 单元 | upsert 去重（同源同 key） | tmp_db | 插入同 (source,source_key) 两遍 | 第二次返回空列表；表内 1 行 |
| T-DB-03 | 单元 | 跨源同文去重 | tmp_db | 插入 title+url 相同的两条不同源 | 仅 1 行入库（content_hash 冲突） |
| T-DB-04 | 单元 | upsert 返回新增 id | tmp_db | 插入 3 条（1 条重复） | 返回 2 个 id，且为新行 id |
| T-DB-05 | 单元 | start_digest_run 幂等 | tmp_db | 同日调用两次 | 返回相同 run id；表内 1 行 |
| T-DB-06 | 单元 | finish_digest_run 记账 | tmp_db | 先 start 再 finish(tokens/cost) | 字段正确落库 |
| T-DB-07 | 单元 | add_digest_items | tmp_db | 添加 3 条 | 3 行；UNIQUE 约束生效 |
| T-DB-08 | 单元 | 反馈去重（同文章多次） | tmp_db | add_feedback like 两次 | 表内 1 行（先删后插） |
| T-DB-09 | 单元 | bump_keyword_weight 钳制 | tmp_db | bump +5 再 bump -10 | 值 ∈ [0.2, 2.0] |
| T-DB-10 | 单元 | get_stats 聚合 | tmp_db | 插入 2 次 run 记录 | 计数/token/成本正确 |
| T-DB-11 | 集成 | 坏路径（目录不存在） | tmp_path 子目录不存在 | Storage(path).init_schema() | 抛 `StorageError` |

### 4.3 采集适配器（test_sources.py，全部 respx mock）

| ID | 级别 | 描述 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|---|
| T-SRC-RSS-01 | 单元 | 正常解析 RSS | mock `sspai.com/feed` → rss_sample.xml | `RssAdapter.fetch` | 2 条 Article，title/url/author 正确 |
| T-SRC-RSS-02 | 单元 | summary 去 HTML | 同上 | 断言 summary | 无 `<p>` 标签，截断 ≤200 字 |
| T-SRC-RSS-03 | 单元 | 缺 guid 用 link | fixture 无 guid 条目 | fetch | source_key == link |
| T-SRC-RSS-04 | 单元 | 单 URL 失败隔离 | 两个 URL，其一 500 | fetch | 返回另一 URL 的条目；异常被吞 |
| T-SRC-RSS-05 | 单元 | 空 feed | mock 空 channel | fetch | 返回 [] |
| T-SRC-RSS-06 | 单元 | 空标题丢弃 | 条目 title 为空 | fetch | 该条被跳过 |
| T-SRC-BILI-01 | 单元 | 正常解析 | mock popular API → bilibili_sample.json | fetch | title/url(BV)/author/source_key(aid) 正确 |
| T-SRC-BILI-02 | 单元 | 非 0 code | mock `{"code":-400}` | fetch | 返回 []（异常已记录） |
| T-SRC-BILI-03 | 单元 | 缺 bvid 条目跳过 | 条目无 bvid | fetch | 该条跳过，其余保留 |
| T-SRC-ZHIHU-01 | 单元 | 正常解析 | mock hot-lists → zhihu_sample.json | fetch | url 已替换为 www.zhihu.com/question/{id}（**单数**，非 questions） |
| T-SRC-ZHIHU-02 | 单元 | 空 data | mock `{"data":[]}` | fetch | 返回 [] |
| T-SRC-ZHIHU-03 | 单元 | 缺 excerpt 容错 | target 无 excerpt | fetch | summary=None，不崩溃 |
| T-SRC-JUEJIN-01 | 单元 | 正常解析 | mock recommend API → juejin_sample.json | fetch | url 为 juejin.cn/post/{id}；author 正确 |
| T-SRC-JUEJIN-02 | 单元 | err_no != 0 | mock `{"err_no":1}` | fetch | 返回 [] |
| T-SRC-JUEJIN-03 | 单元 | 请求头正确 | mock 并捕获请求 | fetch | 断言 Origin/Content-Type/UA 头存在 |
| T-SRC-HN-01 | 单元 | 正常解析 | mock Algolia → hnews_sample.json | fetch | title/url/author/source_key(objectID) 正确；url 为 HN 讨论页 item?id=（2026-09-01 修订） |
| T-SRC-HN-02 | 单元 | url 为空回退 item 页 | fixture 第二条 url=null | fetch | url == https://news.ycombinator.com/item?id=49400000 |
| T-SRC-HN-03 | 单元 | 请求参数正确 + 失败隔离 | mock 并捕获请求 / 500 | fetch | hitsPerPage 传入 cfg；500 时返回 [] |
| T-SRC-INFOQ-01 | 单元 | 正常解析 | mock infoq.cn/feed → infoq_sample.xml | fetch | title/链接正确，source="infoq" |
| T-SRC-INFOQ-02 | 单元 | 空 feed | mock 空 channel | fetch | 返回 [] |
| T-SRC-INFOQ-03 | 单元 | 继承复用 RSS 清洗 | 条目带 HTML summary | fetch | 无标签，截断生效 |
| T-SRC-ALL-01 | 集成 | build_adapters 顺序 | sample_config | 调用 | 顺序 = enabled 列表 |
| T-SRC-ALL-02 | 集成 | 单源超时隔离 | mock 某源挂起（asyncio.sleep > 30s） | run_once 采集阶段 | 该源 30s 超时被掐断，其余源正常 |

### 4.4 LLM 客户端（test_llm.py）

| ID | 级别 | 描述 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|---|
| T-LLM-01 | 单元 | 构造候选 JSON | 2 个 ScoredArticle | `rank()` 内部拼装 | 含 article_id/title/summary/source 四键 |
| T-LLM-02 | 单元 | 合法 JSON 解析 | llm_response_ok.json | `parse_response` | ok=True，picks 含 (1,1) |
| T-LLM-03 | 单元 | 围栏剥离 | llm_response_fence.txt | `parse_response` | ok=True，picks=[] |
| T-LLM-04 | 单元 | 非 JSON 内容 | llm_response_badjson.txt | `parse_response` | ok=False |
| T-LLM-05 | 单元 | article_id 不在候选集 | picks 含 id=999 | `parse_response(valid_ids={1})` | ok=False |
| T-LLM-06 | 单元 | rank 重复 | picks 两条 rank 都为 1 | `parse_response` | ok=False |
| T-LLM-07 | 单元 | 条数超 top_n | picks 5 条但 top_n=3 | `parse_response(top_n=3)` | ok=False |
| T-LLM-08 | 集成 | 请求体与响应透传 | mock /chat/completions | `_chat` | 返回 dict 含 usage；请求含 response_format |
| T-LLM-09 | 集成 | 5xx 重试成功 | mock 先 502 后 200 | `_chat` | 最终成功；调用次数=2 |
| T-LLM-10 | 集成 | 429 重试后放弃 | mock 一直 429 | `_chat` | 重试 3 次后抛 LLMError |
| T-LLM-11 | 集成 | 超时 | mock 挂起超过 timeout | `_chat` | 抛 LLMError（tenacity 3 次后） |
| T-LLM-12 | 单元 | 成本计算 | tokens 1000/80 | 计算 | cost = 0.001*0.66 + 0.00008*1.98 ≈ 0.0008184 |
| T-LLM-13 | 集成 | 输入超长截断 | 候选文本 20k 字符 | `rank()` | 发送文本 ≤ max_input_chars |

### 4.5 排序（test_ranker.py）

| ID | 级别 | 描述 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|---|
| T-RANK-01 | 单元 | 关键词命中加分 | weights {"AI":2.0}，标题含 "AI" | `rule_score` | score = 2.0 + 0(来源) + 时效 + 0 |
| T-RANK-02 | 单元 | 关键词最多计 3 次 | 标题含 5 个 "AI" | `rule_score` | 命中计 3 次（=6.0） |
| T-RANK-03 | 单元 | 时效加成 ≤24h | frozen_now=08-27 08:00，published=08-26 20:00 | `rule_score` | 含 +2.0 |
| T-RANK-04 | 单元 | 时效 24-48h | published=08-25 12:00 | `rule_score` | +1.0 |
| T-RANK-05 | 单元 | 超 48h 无加成 | published=08-20 | `rule_score` | +0 |
| T-RANK-06 | 单元 | 来源权重 | source_weight=1.5 | `rule_score` | 计入 1.5 |
| T-RANK-07 | 单元 | feedback_bias like | 文章有 like 历史 | `rule_score` | +1.0；dislike → -1.0 |
| T-RANK-08 | 单元 | 候选排序截取 | 50 条，max=40 | `select_candidates` | 返回 40 条降序 |
| T-RANK-09 | 单元 | 保底策略 | 全部 0 分 | `select_candidates` | 每源最高分 1 条补齐 |
| T-RANK-10 | 集成 | LLM 成功路径 | mock LLM ok | `rank_and_pick` | picks=10，fallback=False |
| T-RANK-11 | 集成 | LLM 失败降级 | mock LLM ok=False | `rank_and_pick` | 规则分 top10，fallback=True |
| T-RANK-12 | 集成 | LLM 部分非法降级 | picks 只含 3 条合法但 < top_n 且校验失败场景 | `rank_and_pick` | 按设计降级规则处理（ok=False → 规则分） |

### 4.6 简报生成（test_digest.py）

| ID | 级别 | 描述 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|---|
| T-DIGEST-01 | 单元 | 完整格式 | 3 条 items | `build_digest_text` | 含标题行、序号、【来源】、链接 |
| T-DIGEST-02 | 单元 | 摘要截断 60 字 | 长摘要 200 字 | `truncate` | 60 字 + '…' |
| T-DIGEST-03 | 单元 | 来源标签 | source 四类 | 生成 | B站/知乎/掘金/RSS 映射正确 |
| T-DIGEST-04 | 单元 | 空列表 | items=[] | `build_digest_text` | 返回"今日无精选内容"提示文案 |
| T-DIGEST-05 | 单元 | UTF-8 安全截断 | 含中文/emoji 长文 | `truncate_bytes(s, 2048)` | 字节数 ≤2048 且无乱码（能 decode） |
| T-DIGEST-06 | 单元 | reason 为 None 容错 | reason=None | `build_digest_text` | 不崩溃，该行省略理由 |

### 4.7 推送（test_publisher.py）

| ID | 级别 | 描述 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|---|
| T-PUSH-01 | 单元 | wecom 成功 | mock → wecom_ok.json | `WecomPublisher.push` | ok=True，detail 含 errcode 0 |
| T-PUSH-02 | 单元 | wecom 无效 key | mock → wecom_badkey.json | push | ok=False，detail 提示 key |
| T-PUSH-03 | 集成 | wecom 5xx 重试 | mock 502→502→200 | push | 最终 ok=True，请求 3 次 |
| T-PUSH-04 | 集成 | 超长内容按条拆分发送 | 3KB 内容（含多条完整条目） | `WecomPublisher.push` | 请求 2 次（respx 计数）；每块 ≤2048 字节；每块首行为条目开头（无半条） |
| T-PUSH-10 | 单元 | split_digest_blocks 边界 | 标题行 + 6 条长条目（>2048B） | 调用 | 返回 2 块；每块 ≤2048 字节；"1. " 只在第一块首行 |
| T-PUSH-11 | 单元 | 单条超长兜底 | 单条 3KB（无换行） | `split_digest_blocks` | 仍按行切分（单行超长时该行独占一块，不抛异常） |
| T-PUSH-05 | 单元 | serverchan 成功 | mock → serverchan_ok.json | push | ok=True |
| T-PUSH-06 | 单元 | serverchan 失败码 | mock `{"code":1}` | push | ok=False |
| T-PUSH-07 | 单元 | Noop 写文件 | tmp 路径 | `NoopPublisher.push` | 文件内容==content，ok=True |
| T-PUSH-08 | 单元 | factory 路由 | provider 各值 | `create_publisher` | 返回对应类实例 |
| T-PUSH-09 | 单元 | 密钥缺失 | 无 WECOM_WEBHOOK_KEY | 构造 WecomPublisher 并 push | ok=False 而非抛异常（detail 提示配置） |

### 4.8 反馈（test_feedback.py）

| ID | 级别 | 描述 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|---|
| T-FBK-01 | 单元 | like 命中关键词加权 | 文章标题含 "AI"，权重 1.0 | `apply_feedback(like)` | AI 权重 → 1.1（≤2.0） |
| T-FBK-02 | 单元 | dislike 降权 | 同上 | `apply_feedback(dislike)` | AI 权重 → 0.95；文章 state=dismissed |
| T-FBK-03 | 单元 | 权重钳制 | 连续 like 20 次 | 循环调用 | 权重恒 ≤2.0 |
| T-FBK-04 | 单元 | 无命中 + extra_keyword | 标题无关键词，--keyword 开源 | like | 开源 权重 +0.1 并入库 |
| T-FBK-05 | 单元 | 重复反馈覆盖 | like 后 dislike | 两次调用 | 最后状态为 dislike（feedback 表 1 行） |
| T-FBK-06 | 集成 | 反馈影响下次排序 | like 后重新 rule_score | 全流程 | 该文 score 含 +1.0 bias |

### 4.9 调度（test_scheduler.py）

| ID | 级别 | 描述 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|---|
| T-SCHED-01 | 单元 | 解析时间 "08:00" | config | 构造 trigger | hour=8, minute=0, tz=Asia/Shanghai |
| T-SCHED-02 | 单元 | 非法时间 "25:99" | config | 构造 | 抛 ConfigError |
| T-SCHED-03 | 集成 | 到点触发执行 | mock 全 HTTP + 真实调度器(短间隔覆盖) | run_forever 内单次 job | 执行 run_once，digest_runs 有记录 |
| T-SCHED-04 | 集成 | 同日二次触发不重复推送 | 首跑 pushed=1 | 再跑 run_once | 第二次不调 webhook（mock 断言调用数=1） |

### 4.10 E2E（test_e2e.py）

| ID | 级别 | 描述 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|---|
| T-E2E-01 | E2E | dry-run 全流程 | mock 全部源 + LLM + 不推送 | `run_once(dry_run=True)` | 返回 0；logs/last_digest.md 生成且含 10 条或候选数 |
| T-E2E-02 | E2E | 真实推送全流程 | mock 源 + LLM + wecom ok | `run_once()` | 返回 0；digest_runs.pushed=1，channel=wecom |
| T-E2E-03 | E2E | 单源故障不影响 | 知乎源 mock 500 | dry-run | 返回 0；digest 含其余源内容 |
| T-E2E-04 | E2E | LLM 故障降级推送 | LLM mock 500 | `run_once()` | 返回 0；fallback_used=1，仍推送 |
| T-E2E-05 | E2E | 推送失败返回码 | wecom mock 93000 | `run_once()` | 返回 1（部分失败）；日志 ERROR |
| T-E2E-06 | E2E | 全源失败 | 所有源 500 | `run_once(dry_run=True)` | 返回 1；digest 提示无内容 |
| T-E2E-07 | E2E | 幂等：同日两次 run | 第一次成功 | 第二次 run | webhook 仅调用 1 次（respx 计数） |
| T-E2E-08 | E2E | tracking 全链路（dry-run） | 配置 base_url + TRACKING_API_TOKEN，mock clicks 返回 1 条点击 / links 200 | `run_once(dry_run=True)` | 返回 0；点击回写权重 +0.05；简报全部链接为短链；clicks 表 1 行、游标推进 |
| T-E2E-09 | E2E | 短链注册失败 fail-open | mock links 500（重试后仍失败） | run_once | 返回 0；简报保留原始 URL（无 /c/） |
| T-E2E-10 | E2E | 点击同步失败 fail-open | mock clicks 500 | run_once | 返回 0；短链仍注册（简报含 /c/）；游标不动 |
| T-E2E-11 | E2E | tracking 关闭（v1 回归） | base_url 留空 | run_once | 返回 0；links/clicks 请求次数均为 0 |

### 4.11 日志模块（test_log.py）

被测接口（开发文档 §4.2）：`setup_logging(level, log_file, max_bytes, backup_count)`。

| ID | 级别 | 描述 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|---|
| T-LOG-01 | 单元 | 默认配置只有控制台 handler | 无 log_file | `setup_logging()` | 根 logger `daily_picks` 存在；handlers 含 StreamHandler，不含 RotatingFileHandler |
| T-LOG-02 | 单元 | 指定文件后写日志落盘 | tmp_path 下 log_file | `setup_logging(log_file=...)` 后 `logging.getLogger("daily_picks").info("hello")` | 文件存在且内容含 "hello" |
| T-LOG-03 | 单元 | 轮转生效 | max_bytes=100 | 连续写 200 行日志 | 生成 `.1` 备份文件（RotatingFileHandler 轮转） |
| T-LOG-04 | 单元 | 日志格式正确 | 写一条日志 | 读文件首行 | 匹配 `YYYY-MM-DD HH:MM:SS,mmm LEVEL [daily_picks.*] 消息` 模式 |
| T-LOG-05 | 单元 | 重复调用不叠加 handler | 已 setup 两次 | 再 setup 后数 handler | handler 数量不增长（幂等） |

### 4.12 点击追踪（test_tracking.py，全部 respx mock）

被测接口（开发文档 §4.19）：`gen_code / build_tracking_url / TrackingError / TrackingClient.register_links /
TrackingClient.fetch_clicks / apply_click / sync_clicks`。

| ID | 级别 | 描述 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|---|
| T-TRACK-01 | 单元 | 短码格式 | 无 | `gen_code()` | 8 位、仅 base62 字符、两次调用不重复 |
| T-TRACK-01b | 单元 | gen_code 尊重显式长度 | 无 | `gen_code(6)` | 长度 == 6 |
| （未编号） | 单元 | TrackingError 类型 | 无 | issubclass 断言 | TrackingError 是 Exception 子类（调用方 catch 用） |
| T-TRACK-02 | 单元 | 短链构造 | base 带/不带尾斜杠 | `build_tracking_url` | 均为 `{base}/c/{code}`（无重复斜杠） |
| T-TRACK-03 | 单元 | 注册成功 | mock /api/links 200 | `register_links([(42, url)])` | 返回短链；请求含 Bearer 头；body 契约 {code,url,article_id} 正确 |
| T-TRACK-04 | 单元 | 部分失败容错 | 两条链接，其一 400 | `register_links` | 只返回成功项（42），不抛异常 |
| T-TRACK-05 | 集成 | 5xx 重试 | mock 502→502→200 | `register_links` | 最终成功；共请求 3 次（tenacity） |
| T-TRACK-06 | 单元 | 正常响应解析 | mock /api/clicks 200 | `fetch_clicks(0)` | 返回 ClickEvent 列表 + has_more；请求含 Bearer 头 |
| T-TRACK-07 | 单元 | 401 → TrackingError | mock 401 | `fetch_clicks(0)` | 抛 TrackingError（4xx 不重试） |
| T-TRACK-08 | 单元 | 响应结构非法 | mock 200 + clicks 为字符串 | `fetch_clicks(0)` | 抛 TrackingError（"响应非法"） |
| T-TRACK-09 | 单元 | 命中关键词 +delta | 文章标题含 "AI"，权重 1.0 | `apply_click(storage, aid, 0.05)` | 返回 {"updated": ["AI"], "missing": False}；权重 1.05；不写 feedback 表 |
| T-TRACK-10 | 单元 | 未命中/文章缺失容错 | 无命中标题或 id=999 | `apply_click` | 权重不变（1.0）；缺失时 missing=True；均不抛异常 |
| T-TRACK-11 | 集成 | 同步幂等 | 同一批事件同步两次 | `sync_clicks` | 第一次 {"synced": 1, "applied": 1}；第二次 {"synced": 1, "applied": 0}；游标=1 |
| T-TRACK-12 | 集成 | 分页游标 | has_more 两页 | `sync_clicks` | 第二页 after=第一页末 id；游标推进到末位；clicks 表 2 行 |
| T-TRACK-13 | 集成 | 网络失败上抛 | mock 401 | `sync_clicks` | 抛 TrackingError（调用方 fail-open）；游标不动 |

---

## 5. 需求追踪矩阵

| 需求 | 覆盖用例 |
|---|---|
| R-001 多源采集 | T-SRC-RSS-01~06, T-SRC-BILI-01~03, T-SRC-ZHIHU-01~03, T-SRC-JUEJIN-01~03, T-SRC-HN-01~03, T-SRC-INFOQ-01~03, T-SRC-ALL-02 |
| R-002 去重 | T-DB-02, T-DB-03, T-DB-04 |
| R-003 规则打分 | T-RANK-01~09 |
| R-004 LLM 精排 | T-LLM-01~13, T-RANK-10~12 |
| R-005 微信推送 | T-PUSH-01~09 |
| R-006 每日调度 | T-SCHED-01~04, T-E2E-07 |
| R-007 偏好反馈 | T-FBK-01~06 |
| R-008 统计 | T-DB-10 |
| R-009 dry-run | T-E2E-01, T-E2E-03~04 |
| R-010 自检命令 | 手动验收 §7（test llm / test push） |
| R-011 日志 | T-LOG-01~05, T-E2E-03~06（日志断言）+ 手动验收 |
| R-012 点击追踪（注册/短链/拉取/同步） | T-TRACK-01~08, T-E2E-08/10/11, worker node --test（11+4=15 用例） |
| R-013 点击回写偏好（点击→弱 like） | T-TRACK-09~13, T-E2E-08 |
| N-001 成本 | T-LLM-12, T-LLM-13 |
| N-003 可靠性 | T-SRC-ALL-02, T-LLM-09~11, T-PUSH-03, T-E2E-03~06 |
| N-005 可测试性 | 全表（所有外部调用 mock） |

---

## 6. CI 配置（.github/workflows/ci.yml，开源时启用）

```yaml
name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with: { python-version: "3.11" }
      - run: uv sync --dev
      - run: uv run ruff check .
      - run: uv run pytest --cov --cov-report=term-missing
```

---

## 7. 手动验收清单（上线前，真实外部调用）

| # | 步骤 | 命令 | 通过标准 |
|---|---|---|---|
| 1 | 配置 | `uv run daily-picks init`，填写 config.yaml 与 .env | config.yaml 生成；`daily-picks --help` 列出全部子命令 |
| 2 | LLM 自检 | `uv run daily-picks test llm` | 输出"LLM OK"与 token 数 |
| 3 | 推送自检 | `uv run daily-picks test push` | 微信收到"测试消息" |
| 4 | 预览 | `uv run daily-picks run --dry-run` | logs/last_digest.md 有 ≥5 条，格式符合设计文档 §8 |
| 5 | 真实运行 | `uv run daily-picks run` | 微信收到真实推送；`stats` 有成本记录 |
| 6 | 反馈闭环 | `daily-picks feedback like 1` 后 `stats` | 无报错；interest_weights 变化可见 |
| 7 | 连续运行 | `serve` 或 cron 部署，观察 3 天 | 每天 08:00 左右收到推送；日志无未处理异常 |
| 8 | 故障演练 | 断网跑 `run --dry-run` | 优雅失败，退出码非 0 但无 traceback 刷屏 |

### 7.1 点击追踪（真实环境，M9）

- [ ] `cd worker && node --test test/` 全绿
- [ ] 按 worker/README.md 部署：d1 create → schema.sql 执行成功 → secret put → deploy 成功
- [ ] `daily-picks test track` 输出「追踪服务 OK」（未配置时输出「未启用」退出码 1）
- [ ] 真实推送 1 次 → 手机微信点击 2 个链接 → Worker 返回 302 且页面正常打开
- [ ] `daily-picks track sync` 输出「拉取 2 条点击，回写权重 ≥1 条」；再跑一次「回写权重 0 条」（幂等）
- [ ] 次日 `daily-picks run --dry-run`：简报链接为短链；`stats` 后 `sqlite3 data/daily_picks.db 'SELECT * FROM clicks;'` 可见 2 行

---

## 8. 定义完成（DoD）

- [ ] `uv run pytest` 全绿（含 T-E2E-01~11 与 T-TRACK-01~13）
- [ ] 覆盖率 ≥85%（核心模块，`--fail-under 85` 通过）
- [ ] `uv run ruff check .` 零告警
- [ ] `daily-picks init` + `test llm` + `test push` 三步可复现
- [ ] `run --dry-run` 输出符合设计文档 §8 格式
- [ ] 手动验收清单 1-8 全部通过
- [ ] README（安装/配置/使用/架构图/截图）+ config.example.yaml + .env.example 已提交
