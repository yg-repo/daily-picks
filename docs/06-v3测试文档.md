# DailyPicks v3 测试文档（docs/06）

> 对应设计文档 docs/04、开发文档 docs/05。测试文件按里程碑命名：`tests/test_m10_setup.py`、`tests/test_m11_deep.py`、`tests/test_m12_feedback.py`、`tests/test_m13_evolve.py`。LLM 全部 mock（不走网络），mock 方式参照 `tests/test_ranker.py` 的 FakeLLM。
> 完成标准：全量 `pytest` 全绿 + 覆盖率 ≥85%（核心模块）+ ruff 零告警。

## 1. M10 用例（test_m10_setup.py）

| 编号 | 用例 | 输入/操作 | 断言 |
|---|---|---|---|
| T-SETUP-01 | 默认标签展示 | 调用 choose_tags（monkeypatch input 返回空） | 返回 DEFAULT_TAGS 前 3 个 |
| T-SETUP-02 | 序号多选 | input="1,3,5" | 返回对应 3 个标签 |
| T-SETUP-03 | 自定义标签 | input="自定义:量子计算" | 结果含"量子计算" |
| T-SETUP-04 | 来源推荐内置映射 | tags=["AI大模型"] | 结果含 hnews/infoq |
| T-SETUP-05 | LLM 来源推荐 | FakeLLM 返回 {"sources":[...]} | source_registry 有注册，key=rss:机器之心 |
| T-SETUP-06 | LLM 推荐非法 url | url="not-a-url" | 该源被跳过不注册 |
| T-SETUP-07 | 条数默认 | input="" | top_n=5 |
| T-SETUP-08 | 条数越界重输 | input="99\n3" | 最终 top_n=3 |
| T-SETUP-09 | 完整向导写库 | run_setup（全部 mock input） | user_profile 单行、tags/sources JSON 正确 |
| T-SETUP-10 | 幂等重跑 | 连续两次 run_setup | 第二次覆盖更新，仍单行（id=1） |
| T-SETUP-11 | 无 LLM 降级 | llm=None | recommend_sources 仅内置映射，不抛错 |
| T-SETUP-12 | save_profile clamp | top_n=0 / 99 | 入库时被 clamp/拒绝（值校验） |

## 2. M11 用例（test_m11_deep.py）

| 编号 | 用例 | 输入/操作 | 断言 |
|---|---|---|---|
| T-DEEP-01 | 深度评分有效 | FakeLLM 返回 {"deep_score":78,"keywords":["A","B","C"],"reason":"..."} | DeepResult.deep_score==78, ok=True |
| T-DEEP-02 | 评分越界回退 | deep_score=150 | ok=False |
| T-DEEP-03 | 关键词不足 | keywords=["A"] | keywords 保留原值，ok=False（fail-open 依据） |
| T-DEEP-04 | 理由含禁用词 | reason 含"深入浅出" | ok=False（prompt 约束的代码侧校验） |
| T-DEEP-05 | 批量过滤保高分 | 3 篇：78/55/30，threshold=60 | 返回 [78 那篇]，DeepResult 3 条全量 |
| T-DEEP-06 | fail-open 保留失败篇 | 1 篇 ok=False（超时 mock） | 该篇保留在候选 |
| T-DEEP-07 | 过滤后不足降阈值 | 全部 <60 但 ≥50，top_n=5 | 阈值降为 50 重试，返回 ≥50 的 |
| T-DEEP-08 | 并发上限 | 5 篇 | gather 并发 ≤3（用计数 mock 验证信号量） |
| T-DEEP-09 | format_keywords | ["A","B","C","D","E","F"] | "A、B、C、D、E"（截断 5） |
| T-DEEP-10 | 模板兜底摘要 | deep_map 缺该 article_id | 行内出现"摘要：" |
| T-DEEP-11 | 编造关键词剔除（2026-09-04） | keywords 全不在原文 | 校验后 []（模板走摘要兜底） |
| T-DEEP-12 | 部分编造关键词过滤 | 3 个中 1 个编造 | 只保留原文命中项 |
| T-DEEP-13 | 正文抓取触发（2026-09-04） | summary<200 字 + client | fetch 被调、body_used=True、正文进 user prompt |
| T-DEEP-14 | summary 足长不抓正文 | summary≥200 字 | fetch 不被调、body_used=False |
| T-DEEP-15 | HTML 清洗 | 含 script/style/nav | 纯文本无标签无脚本 |
| T-DEEP-16 | 抓取失败 fail-open | 500/非 HTML | 返回 "" 不抛错 |
| T-DEEP-17 | deep 结果落库（2026-09-04） | run_once v3 + 写回 | digest_items.deep_* 列有值 |

## 3. v3 模板用例（test_m11_deep.py 或 test_digest_v3.py）

| 编号 | 用例 | 断言 |
|---|---|---|
| T-DG3-01 | 完整条目格式 | 含【来源】标题 / 关键词：/ 推荐理由：/ 链接： |
| T-DG3-02 | 条数头部 | "今日深度精选（2条）" |
| T-DG3-03 | 作者行 | author 非空时出现"作者：xx"，空时无此行 |
| T-DG3-04 | source_display_name | hnews→Hacker News；rss 自定义→registry 名 |

## 4. M12 用例（test_m12_feedback.py）

| 编号 | 用例 | 输入/操作 | 断言 |
|---|---|---|---|
| T-FB-01 | 意图 like | "这条不错" | intent=like（mock LLM 决定） |
| T-FB-02 | 意图 expand | "多推点AI硬件" | intent=expand, tags=["AI硬件"] |
| T-FB-03 | 意图 adjust | "每天推3条就行" | intent=adjust, top_n=3 |
| T-FB-04 | 无 LLM 启发式兜底 | "不要推游戏了" | intent=dislike |
| T-FB-05 | 落库 | apply_feedback | feedback_text 有记录，channel=hermes |
| T-FB-06 | expand 写标签 | tags=["AI硬件"] | tag_weights 有 AI硬件 weight=1.5 source=feedback |
| T-FB-07 | expand 合并进 profile | 原 tags=["AI大模型"] | user_profile.tags 含两标签 |
| T-FB-08 | adjust 更新 top_n | top_n=3 | user_profile.top_n=3，tags/sources 不变 |
| T-FB-09 | 关键词写权重 | keywords=["AI硬件"] | interest_weights 增/加权，clamp [0.2,2.0] |
| T-FB-10 | CLI 文字路由 | `feedback "多推点AI"` | 走文字路径（cmd_feedback 分支） |
| T-FB-11 | CLI 原用法兼容 | `feedback like 42` | 走原 like 逻辑 |
| T-FB-12 | 解析失败不抛 | FakeLLM 返回非法 JSON | intent=none 落库，命令 exit 0 |

## 5. M13 用例（test_m13_evolve.py）

| 编号 | 用例 | 输入/操作 | 断言 |
|---|---|---|---|
| T-EV-01 | 点击演化 | clicks 新增 1 条，文章含关键词"AI" | interest_weights["AI"] +0.05 |
| T-EV-02 | 游标幂等 | evolve 两次 | 第二次不重复加权重（游标推进） |
| T-EV-03 | 标签演化 | expand 反馈未处理 | 标签关键词 +0.1 |
| T-EV-04 | 权重 clamp | 连续演化超 2.0 | 封顶 2.0 |
| T-EV-05 | 集成到 run_once | run_once 流程 | evolve 在打分前调用（mock 验证调用点） |

## 6. 回归（不新增文件）

- 全量 242 例（v1/v2）不得回归：`uv run pytest -q`
- 覆盖率高线不变：TOTAL ≥ 85%（核心模块单测 ≥85%）
- `daily-picks run --dry-run`（真实 LLM 可选）端到端 v3 格式 smoke
- 旧命令兼容：init/run/serve/feedback like/stats/test/track sync 全部可用

## 8. 手动验收清单（每里程碑收尾跑一遍）

```bash
cd ~/daily-picks
uv run pytest -q                                   # 全绿
uv run pytest --cov=daily_picks --cov-report=term-missing | grep TOTAL
uv run ruff check daily_picks/ tests/
uv run daily-picks setup <<< $'1,2\n\n3\n'         # M10：管道输入走通向导（临时库防污染）
uv run daily-picks feedback "多推点AI硬件"          # M12：文字反馈落库
uv run daily-picks stats                           # 输出含 v3 计数
```

- 真实数据验证用备份库：`mv data/daily_picks.db data/daily_picks.db.bak` 后重建（幂等采集会 dedupe，旧库 dry-run 候选为空）

## 7. v3 补充用例清单（M10-M13 实现期新增，未编号于上表）

| 文件 | 用例 | 断言要点 |
|---|---|---|
| test_m10_setup.py | TestV3Config | profile/feedback 配置默认值、越界校验、save_config 回环 |
| test_m10_setup.py | TestProfileStorage | 画像单行幂等、tag 权重 clamp、源注册 |
| test_m10_setup.py | TestChooseTags/TestChooseTopN 边界 | 混合输入、全非法回退默认 |
| test_m10_setup.py | TestLlmChat | chat 围栏剥离与消息构造 |
| test_m11_deep.py | TestDeepAnalyze 边界 | 非数字评分、空 reason 回退、非法 JSON |
| test_m11_deep.py | TestDeepFilter 边界 | 空候选、weights 透传、超时不重复调 LLM |
| test_m11_deep.py | TestRunOnceV3 | v3 deep 路径出 v3 简报；无 key 跳过 deep |
| test_m12_feedback.py | TestFeedbackChannel | 抽象类不可实例化、Hermes 占位行为 |
| test_m12_feedback.py | TestParseFeedback 启发式边界 | adjust/expand/none 启发式分支 |
| test_m12_feedback.py | TestApplyTextFeedback 边界 | 已有标签不动、无画像 adjust noop、clamp |
| test_m12_feedback.py | TestApplyFeedbackDispatch | v1 签名回归（分派器兼容） |
| test_m13_evolve.py | TestBumpKeywords | 命中/大小写/未命中不动 |
| test_m13_evolve.py | TestEvolveWeights 补充 | sync 游标协作不双重回写、非 expand 不演化 |
| test_m13_evolve.py | TestEvolveIntegration | profile 关闭不演化 |
| test_m13_evolve.py | TestStatsV3 | stats 输出 v3 计数 |
