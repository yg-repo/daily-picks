# DailyPicks v3 设计文档 — 深度内容精选（docs/04）

> 版本：v3.0.0 草案 ｜ 日期：2026-08-31 ｜ 关联：docs/01（v1/v2 基准）、docs/05（开发文档）、docs/06（测试文档）
> 本文件是 v3 的唯一事实来源。所有函数签名、表字段、配置键以本文为准，实现禁止自行改名（AGENTS.md 第 4 条）。

## 1. 背景与目标

v1/v2 的定位是"聚合 + 精选"：6 个内容源海量采集 → 规则分 + LLM 精排 → 推 10 条。用户实际使用后反馈核心问题：
**信息太杂、太浅**——RSS 聚合内容居多，缺乏有深度思考的内容，与"快节奏时代看深度内容"的诉求不符。

v3 定位转变：**少而精的深度内容**。不追求覆盖量，追求每条都有思考深度。

### 1.1 目标（非功能）

| 编号 | 目标 |
|---|---|
| G-01 | 每天推送 5 条（默认）深度内容，宁缺毋滥 |
| G-02 | 推送格式：来源+标题、关键词（替代摘要）、具体推荐理由、链接/作者 |
| G-03 | 用户首次启动时通过向导完成标签/信息源/条数配置，可随时重跑 |
| G-04 | 点击与反馈（含文字反馈）持续演化筛选条件与标签，不局限于初始选择 |

### 1.2 边界（不做）

- 不做 App/网页端（保持微信文字推送）
- 不做内容生产（纯聚合与精选）
- 企业微信双向回调（需求 5 的通道 2）**本期不做**，但反馈入口必须做成适配器，后期可无缝切换

## 2. 需求规格（用户需求 → R-编号）

| 编号 | 需求 | 来源 |
|---|---|---|
| R-V3-01 | 内容深度优先：候选需经"深度评分"，低于阈值不推送 | 需求 1 |
| R-V3-02 | 每日条数默认 5，可自定义（范围 1–10） | 需求 3.3 |
| R-V3-03 | 启动向导：标签选择（默认标签多选 + 自定义） | 需求 3.1 |
| R-V3-04 | 启动向导：信息源选择（按标签推荐 + 自定义） | 需求 3.2 |
| R-V3-05 | 推送格式：保留【来源】标题；摘要→关键词；推荐理由具体化；链接/作者不变 | 需求 4 |
| R-V3-06 | 反馈通道：文字反馈入口（Hermes 通道），可扩展为企微回调 | 需求 5 |
| R-V3-07 | 点击/反馈 → 更新筛选条件（权重演化） | 需求 6.1 |
| R-V3-08 | 反馈 → 扩展标签（不局限于初始标签） | 需求 6.2 |
| R-V3-09 | 兼容 v1/v2：现有源、点击追踪、定时服务不破坏 | 需求 — |

## 3. 总体架构

```
┌──────────────────────── 新增（v3） ────────────────────────┐
│  setup.py     启动向导：标签/来源/条数 → user_profile 表      │
│  feedback.py  反馈解析（LLM 意图+标签提取）→ 权重/标签演化     │
│  deep.py      深度评分 + 关键词提取（LLM，M11 核心）           │
│  digest_v3.py v3 推送模板（关键词行替代摘要行）                │
└────────────────────────────────────────────────────────────┘
┌──────────────────────── 改造（v3） ────────────────────────┐
│  ranker.py   （未改动）select_candidates 仅非 deep 路径执行          │
│  cli.py       新增 setup / feedback 子命令（feedback 扩展文字） │
│  config.py    RootConfig 增加 profile 段                       │
│  cli.py       run_once 增加 deep 阶段（集成点，docs/05 §2.2）       │
└────────────────────────────────────────────────────────────┘
┌──────────────────────── 复用（v1/v2） ─────────────────────┐
│  sources/ 采集（6 源 + rss 自定义源）  tracking.py 点击追踪      │
│  llm.py LLM 客户端  storage.py SQLite  publisher.py 推送        │
│  scheduler.py 定时  digest.py（v1/v2 模板保留兼容）              │
└────────────────────────────────────────────────────────────┘
```

### 3.1 流水线变化（run 流程）

v2: 采集 → 去重入库 → 规则打分 → select_candidates → LLM 精排 → 推送
v3: 采集 → 去重入库 → 规则打分 → **deep 阶段（LLM 深度评分+关键词，过滤 < 阈值）** → select_candidates(min_score) → LLM 精排 → **v3 模板推送**

deep 阶段只对规则分 Top-N 候选执行（控制 LLM 成本，N = max_candidates）。

## 4. 数据模型（新表，DDL 锁定）

```sql
-- 用户画像（setup 向导产物，单用户单行）
CREATE TABLE IF NOT EXISTS user_profile (
    id          INTEGER PRIMARY KEY CHECK (id = 1),   -- 强制单行
    tags        TEXT NOT NULL DEFAULT '[]',           -- JSON 数组，如 ["AI大模型","创业"]
    sources     TEXT NOT NULL DEFAULT '[]',           -- JSON 数组，源 key 列表（含自定义）
    top_n       INTEGER NOT NULL DEFAULT 5 CHECK (top_n BETWEEN 1 AND 10),
    updated_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- 标签权重（点击/反馈/手动演化，驱动关键词与来源选择）
CREATE TABLE IF NOT EXISTS tag_weights (
    tag        TEXT PRIMARY KEY,
    weight     REAL NOT NULL DEFAULT 1.0,             -- 范围 [0.2, 2.0]
    source     TEXT NOT NULL DEFAULT 'manual',        -- 'manual'|'click'|'feedback'
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- 文字反馈记录（Hermes 通道 / 未来企微回调）
CREATE TABLE IF NOT EXISTS feedback_text (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    raw_text        TEXT NOT NULL,                    -- 用户原始反馈文字
    intent          TEXT NOT NULL CHECK (intent IN ('like','dislike','expand','adjust','none')),
    article_id      INTEGER,                          -- 可选：针对某条
    extracted_tags  TEXT NOT NULL DEFAULT '[]',       -- JSON 数组：反馈中提取的新标签
    keywords        TEXT NOT NULL DEFAULT '[]',       -- JSON 数组：提取的关键词（进 interest_weights）
    channel         TEXT NOT NULL DEFAULT 'hermes',   -- 'hermes'|'wecom'
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- 自定义信息源注册表（setup 生成 + 运行维护）
CREATE TABLE IF NOT EXISTS source_registry (
    key        TEXT PRIMARY KEY,                      -- 源 key（rss 自定义源的唯一标识）
    name       TEXT NOT NULL,                         -- 显示名
    kind       TEXT NOT NULL DEFAULT 'rss',           -- 'rss'|'builtin'
    url        TEXT,                                  -- rss url（kind='rss' 必填）
    tags       TEXT NOT NULL DEFAULT '[]',            -- JSON 数组：关联标签
    enabled    INTEGER NOT NULL DEFAULT 1,
    added_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

### 4.1 兼容性

- articles/feedback/interest_weights/clicks/meta/digest_* 全部不动
- feedback 表（like/dislike）保留；feedback_text 是新入口，处理后同样写 interest_weights

## 5. 配置设计（config.yaml v3 段）

```yaml
profile:
  enabled: true            # false = 完全走 v2 行为
  top_n: 5                 # 每日推送条数，setup 写入，范围 1-10
  deep_threshold: 60       # 深度评分阈值（0-100），低于此值不推送
  deep_candidates: 20      # deep 阶段最多评分的候选数（LLM 成本控制；8/31 实测 40 耗时过长调至 20）
  tags: []                 # setup 写入（JSON 数组，实际存 user_profile 表）
  sources: []              # setup 写入
feedback:
  channel: hermes          # 'hermes'（本期）| 'wecom'（后期）
  extract_keywords: true   # 反馈文字是否提取关键词写入 interest_weights
```

`config.py` 新增：

```python
class ProfileConfig(BaseModel):
    enabled: bool = False          # v3 默认关，setup 后开
    top_n: int = 5
    deep_threshold: int = 60
    deep_candidates: int = 40
    tags: list[str] = []
    sources: list[str] = []

class FeedbackConfig(BaseModel):
    channel: str = "hermes"
    extract_keywords: bool = True

class RootConfig(BaseModel):  # 追加
    profile: ProfileConfig = ProfileConfig()
    feedback: FeedbackConfig = FeedbackConfig()
```

## 6. 模块设计与函数签名（锁定）

### 6.1 `daily_picks/setup.py` — 启动向导

```python
DEFAULT_TAGS: list[str] = [
    "AI大模型", "编程开发", "创业商业", "投资经济", "人文历史", "个人成长",
]
# 默认标签 → 内置源推荐（key 见 §6.5）
TAG_SOURCE_MAP: dict[str, list[str]] = {
    "AI大模型": ["infoq"],
    "编程开发": ["juejin", "rss:阮一峰"],
    "创业商业": [],
    "投资经济": [],
    "人文历史": [],
    "个人成长": ["rss:少数派"],
}
# 修订说明（2026-09-01）：仅保留实测有效的源——hnews 停用（国内不可达，§6.5）；
# 36氪/虎嗅/机器之心等国内平台公开 RSS 已关闭（返回 HTML/404），从内置推荐移除。
# 内置 rss 源 url 由 setup.py 的 SOURCE_URLS 提供，recommend_sources 注册到 source_registry
# （2026-09-01 修复：此前只列 key 不注册，内置推荐源实际采集不到）。

async def run_setup(cfg: RootConfig, storage: Storage, llm: LLMClient | None) -> int:
    """交互式向导：① 标签多选/自定义 → ② 来源推荐/增删 → ③ 条数。
    全程 input()/print()；结果写入 user_profile 表 + config.yaml（profile 段）。幂等可重跑。"""

def choose_tags(llm: LLMClient | None) -> list[str]:
    """展示 DEFAULT_TAGS 多选（逗号分隔序号），支持 '自定义:xxx' 追加。返回最终标签列表。"""

async def recommend_sources(tags: list[str], llm: LLMClient | None, storage: Storage) -> list[str]:
    """tags ∩ TAG_SOURCE_MAP 的并集为推荐源；llm 非空时调用 _llm_recommend 补充。"""

async def _llm_recommend(tags: list[str], llm: LLMClient, storage: Storage) -> list[str]:
    """LLM 按标签推荐信息源（JSON 输出 source_name/url 列表），结果写 source_registry。"""

def choose_top_n() -> int:
    """输入每日条数，默认 5，范围 1-10（越界重输）。"""
```

- 修订说明（2026-08-31）：run_setup 为协程——docs/05 §1.3 以 asyncio.run 调用，
  且内部需 await _llm_recommend（LLM 调用）。实现以 async 为准。
- 修订说明（2026-08-31）：recommend_sources/_llm_recommend 为协程并增 storage 参数——
  注册 source_registry 需要存储句柄，且调用 LLM 需 await。

### 6.2 `daily_picks/deep.py` — 深度评分 + 关键词提取（M11 核心）

```python
DEEP_SCORE_MIN: float = 0.0    # LLM 深度评分（0-100）输出下限校验
KEYWORDS_MIN: int = 3
KEYWORDS_MAX: int = 5

@dataclass
class DeepResult:
    article_id: int
    deep_score: int             # 0-100
    keywords: list[str]         # 3-5 个
    reason: str                 # 具体推荐理由（引用文章细节，禁止空泛）
    ok: bool                    # LLM 输出有效？
    body_used: bool = False     # 分析是否基于抓取的正文（True）或仅 title+summary（False）

# 正文抓取（2026-09-04 修复：deep_analyze 此前只喂 title+短摘要，LLM 基于标题联想编造
# 关键词/理由——用户读原文对不上。summary < FETCH_BODY_MIN_SUMMARY 且 url 可抓时，抓正文辅助分析）
FETCH_BODY_MIN_SUMMARY: int = 200   # summary 短于此字数触发正文抓取
FETCH_BODY_MAX_CHARS: int = 3000    # 正文送入 LLM 的最大字符数
FETCH_BODY_TIMEOUT_S: float = 10.0

async def fetch_article_text(url: str, client: httpx.AsyncClient,
                             max_chars: int = FETCH_BODY_MAX_CHARS) -> str:
    """抓取 url 正文并清洗为纯文本（去 script/style/标签，压缩空白，截断 max_chars）。
    失败/非 HTML/超时 → 返回 ""（调用方降级为仅 title+summary，fail-open）。"""

def _strip_html(html: str) -> str:
    """HTML → 纯文本：去 script/style/nav/footer，去标签，解实体，压缩空白。"""

def _validate_keywords(keywords: list[str], text: str) -> list[str]:
    """代码侧可验证校验（2026-09-04 修复）：剔除未在 title+summary+正文中出现的
    关键词（LLM 编造防护）；全部未命中 → 返回 []（推送模板走摘要兜底，docs/06 T-DEEP-11）。"""

async def deep_analyze(article: Article, llm: LLMClient,
                       weights: dict[str, float]) -> DeepResult:
    """LLM 深度分析：输入 title+summary+url，输出 {deep_score, keywords, reason}。
    reason 要求：必须引用文章的具体观点/数据/场景，禁止 '本文深入浅出' 类空话。"""

async def deep_filter(candidates: list[ScoredArticle], llm: LLMClient,
                      threshold: int,
                      weights: dict[str, float] | None = None) -> tuple[list[ScoredArticle], list[DeepResult]]:
    """批量 deep_analyze（并发 ≤3），保留 deep_score >= threshold 的候选。
    失败/超时的文章降级为保留（fail-open，不因 deep 故障丢候选）。
    返回 (过滤后候选, 全量 DeepResult 列表)。"""

def format_keywords(keywords: list[str]) -> str:
    """'k1、k2、k3' 顿号拼接，超 5 个截断。"""
```

- 修订说明（2026-08-31）：deep_filter 增可选参数 weights（透传 deep_analyze 的用户兴趣；
  3 参调用保持合法）；降阈值判定改用模块常量 DEEP_MIN_COUNT = 5（对齐 profile.top_n 默认值，
  签名无 top_n 参数）；集成点在 cli.py run_once（§3 架构图注记的 pipeline.py 不存在，
  docs/05 §2.2 已允许 run_once 方案）；v3 路径中 deep 过滤后的候选直接进入 LLM 精排
  （select_candidates 仅在非 deep 路径执行，实现口径以 docs/05 §2.2 代码为准）。

### 6.3 `daily_picks/feedback.py` — 反馈解析与演化（M12/M13）

```python
FEEDBACK_INTENTS = ("like", "dislike", "expand", "adjust", "none")

@dataclass
class ParsedFeedback:
    raw: str                    # 原始反馈文字（落 feedback_text.raw_text）
    intent: str                 # 见 FEEDBACK_INTENTS
    article_id: int | None
    tags: list[str]             # 提取的新标签（intent=expand）
    keywords: list[str]         # 提取的关键词（写 interest_weights）
    top_n: int | None           # intent=adjust 时的新条数

async def parse_feedback(raw: str, llm: LLMClient) -> ParsedFeedback:
    """LLM 解析文字反馈：意图识别 + 标签/关键词提取（JSON 输出）。"""

def apply_feedback(fb: ParsedFeedback, storage: Storage,
                   extract_keywords: bool = True) -> None:
    """落库 feedback_text + 演化：
    - like/dislike：原文 article_id 命中 → 写 feedback 表（复用 v1 逻辑）+ tag_weights 联动
    - expand：新标签写 tag_weights(source='feedback')，并扩 interest_weights
    - adjust：top_n 写 user_profile
    - 关键词：extract_keywords 时写 interest_weights（clamp [0.2, 2.0]）"""

def evolve_weights(storage: Storage) -> None:
    """点击/反馈驱动的权重演化（M13）：
    1. clicks 增量 → 命中文章 title+summary 关键词 +0.05（复用 tracking.apply_click 逻辑）
    2. feedback_text.intent='expand' 的标签 → 对应关键词权重 +0.1
    3. 所有权重 clamp [0.2, 2.0]"""
```

- 修订说明（2026-08-31）：ParsedFeedback 增 raw 字段（feedback_text.raw_text 为 NOT NULL，
  落库需要原文）；apply_feedback 为同名分派——首参为 ParsedFeedback → v3 文字反馈路径；
  否则保持 v1 签名 apply_feedback(storage, article_id, kind, extra_keyword=None) 行为不变
  （R-V3-09 兼容，docs/05 §3.2）。

### 6.4 `daily_picks/digest_v3.py` — 推送模板

```python
def build_digest_v3(items: list[Pick], articles: dict[int, Article],
                    deep_map: dict[int, DeepResult]) -> str:
    """v3 模板（每条）：
    【{source_name}】{title}
    关键词：{k1}、{k2}、{k3}
    推荐理由：{reason}
    链接：{url}
    {作者行}
    （条目间空一行；总条数 = len(items) ≤ profile.top_n）
    头部：'📚 今日深度精选（{n}条）'"""

def source_display_name(source_key: str) -> str:
    """内置源中文名映射；rss 自定义源查 source_registry.name。"""
```

### 6.5 源 key 与内置源名（锁定）

| key | 名称 | 类型 |
|---|---|---|
| hnews | Hacker News | builtin |
| infoq | InfoQ | builtin |
| juejin | 掘金 | builtin |
| bilibili | B站 | builtin |
| zhihu | 知乎热榜 | builtin |
| rss | 通用 RSS（用户自定义，多实例） | rss |
| rss:机器之心 | 机器之心 | rss |
| rss:阮一峰 | 阮一峰博客 | rss |
| rss:36氪 | 36氪 | rss |
| rss:虎嗅 | 虎嗅网 | rss |
| rss:华尔街见闻 | 华尔街见闻 | rss |
| rss:雪球 | 雪球 | rss |
| rss:知乎日报 | 知乎日报 | rss |
| rss:豆瓣书评 | 豆瓣读书 | rss |
| rss:少数派 | 少数派 | rss |
| rss:得到 | 得到 | rss |

- `rss:` 前缀源通过 source_registry 注册，采集走通用 RSS 适配器（现有 `rss` 适配器）
- 修订说明（2026-08-31 补做）：**registry 驱动采集已实现**——`build_adapters(cfg, storage)` 在 storage 非空时，
  读取 source_registry 中 kind='rss' 且 enabled=1 的 url，合并进 rss 适配器采集列表
  （`RssAdapter(extra_urls=...)`，与 config.yaml `rss.urls` 并集去重）。用户向导选择的源**真正影响采集**。
- 内置源（hnews/infoq/juejin/bilibili/zhihu）继续用现有适配器，tags 可关联

## 7. 推送格式规格（v3 模板）

```
📚 今日深度精选（5条）
──────────────
【Hacker News】标题：xxx
关键词：A、B、C
推荐理由：文章用 X 数据论证了 Y 观点，其中 Z 的对比特别值得注意……
链接：https://...
作者：xxx
──────────────
（后续条目同上）
```

- 理由约束：≥2 句话；必须包含文章具体信息（数据/观点/场景）；禁用词列表写死在 prompt：`深入浅出|受益匪浅|干货满满|值得一读|不容错过`
- 关键词：3–5 个，名词短语优先，来自 title+summary 原文

## 8. 反馈通道抽象（R-V3-06）

```python
class FeedbackChannel(ABC):            # daily_picks/feedback_channels.py
    name: str
    @abstractmethod
    async def receive(self) -> list[RawFeedback]: ...   # 拉取未处理反馈
    @abstractmethod
    async def acknowledge(self, fb_id: str) -> None: ... # 标记已处理

@dataclass
class RawFeedback:
    text: str
    article_id: int | None = None
    channel: str = "hermes"
```

- v3 实现 `HermesChannel`：CLI 命令 `daily-picks feedback "<文字>"` 直接入 feedback_text（零部署）
- 后期实现 `WecomChannel`：企微回调 → 同样入 feedback_text，**无需改演化逻辑**
- CLI：`daily-picks feedback "多推点AI硬件"` 与现有 `feedback like <id>` 共存（argparse 子命令自动路由：纯文字 → 文字反馈；`like/dislike <id>` → 原逻辑）

## 9. 里程碑规划（M10–M13，每步验收标准）

### M10 用户画像与启动向导
- [ ] user_profile / tag_weights / source_registry 表（§4 DDL）
- [ ] config.py ProfileConfig/FeedbackConfig
- [ ] setup.py 全函数 + `daily-picks setup` 命令
- [ ] 测试：setup 模拟输入（monkeypatch input）、表写入、幂等重跑
- 验收：`pytest` 全绿；`daily-picks setup` 用管道输入跑通；覆盖率 ≥85%

### M11 深度选材与 v3 推送
- [ ] deep.py 全函数 + pipeline 集成（deep 阶段）
- [ ] digest_v3.py + run 输出切换（profile.enabled 时用 v3 模板）
- [ ] LLM prompt（深度评分/关键词/理由，含禁用词）
- [ ] 测试：mock LLM 深度过滤/关键词/理由格式/模板快照
- 验收：`pytest` 全绿；`run --dry-run`（mock LLM）输出 v3 格式；覆盖率 ≥85%

### M12 反馈闭环（Hermes 通道）
- [ ] feedback.py parse/apply + feedback_channels.py（HermesChannel）
- [ ] CLI `daily-picks feedback "<文字>"`（与 like/dislike 共存）
- [ ] 测试：意图解析/标签提取/落库/权重 clamp
- 验收：`pytest` 全绿；实测 `feedback "多推点AI硬件"` → feedback_text + tag_weights 有记录

### M13 自适应演化 + 收尾
- [ ] evolve_weights 集成（点击+反馈 → 权重/标签演化）
- [ ] docs/05 开发文档、docs/06 测试文档补齐 v3 章节
- [ ] AGENTS.md v3 指引；README v3 说明
- 验收：全量 pytest + 覆盖率 ≥85%（fail-under 不变）；`run --dry-run` 端到端 v3 格式；git log 里程碑提交清晰

## 10. 风险与降级

| 风险 | 降级 |
|---|---|
| deep 阶段 LLM 失败 | fail-open：deep 失败文章保留为候选（§6.2） |
| deep 过滤后候选 < top_n | 降低阈值重试一次（threshold-10）；仍不足则按现有数量推送（宁缺毋滥） |
| 自定义 RSS 源不可达 | 复用现有失败隔离，不影响其他源 |
| 无 LLM 密钥 | setup 降级为纯内置映射（无 LLM 推荐）；deep 阶段跳过 → 行为等同 v2 |
| 文字反馈解析失败 | intent='none' 落库不演化，不抛错 |
